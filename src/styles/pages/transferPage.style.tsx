import styled from "styled-components";
import { HomeContainer } from "./home.style";

const TransferContainer = styled(HomeContainer)`
    padding-top: 90px;
    justify-content: flex-start;
`;

export { TransferContainer };
